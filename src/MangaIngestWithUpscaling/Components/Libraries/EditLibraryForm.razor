@using MangaIngestWithUpscaling.Components.FileSystem
@using MangaIngestWithUpscaling.Components.Libraries.Filters
@using MangaIngestWithUpscaling.Configuration
@using MangaIngestWithUpscaling.Data.LibraryManagement
@using MangaIngestWithUpscaling.Shared.Data.Analysis
@using MangaIngestWithUpscaling.Shared.Helpers
@using Microsoft.Extensions.Options
@using Humanizer
@using System.Globalization
@inject ApplicationDbContext DbContext
@inject IOptions<KavitaConfiguration> KavitaConfig
@inject IDialogService DialogService
@inject IStringLocalizer<EditLibraryForm> Loc

<MudForm Model="@Library" IsValid="@IsValid">
    <MudTabs Elevation="2" Rounded="true" ApplyEffectsToContainer="true" TabPanelsClass="pa-6">

        <!-- Basic Settings Tab -->
        <MudTabPanel Text="@Loc["Tab_BasicSettings"]" Icon="@Icons.Material.Filled.Settings">
            <MudGrid Spacing="4">
                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_LibraryInfo"]</MudText>
                    <MudTextField Required="true" Label="@Loc["Label_LibraryName"]"
                        HelperText="@Loc["Helper_LibraryName"]" Immediate="true" @bind-Value="Library.Name"
                        @bind-Value:after="OnInputChanged" Variant="Variant.Outlined" />

                    @if (Library.Id > 0)
                    {
                        <MudDivider Style="margin: 16px 0;" />
                        <MudStack Row Spacing="4" Class="mt-2">
                            <MudText Typo="Typo.caption" Color="Color.Secondary">
                                @Loc["Label_Created"] @Library.CreatedAt.ToString("yyyy-MM-dd HH:mm")
                            </MudText>
                            <MudText Typo="Typo.caption" Color="Color.Secondary">
                                @Loc["Label_Modified"] @Library.ModifiedAt.ToString("yyyy-MM-dd HH:mm")
                            </MudText>
                        </MudStack>
                    }
                </MudItem>

                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_IngestConfig"]</MudText>
                    <MudText Typo="Typo.body2" Class="mb-4">@Loc["Body_IngestConfig"]</MudText>
                    <MudStack Spacing="4">
                        @for (int i = 0; i < Library.IngestPaths.Count; i++)
                        {
                            int index = i;
                            LibraryIngestPath ingestPath = Library.IngestPaths[index];
                            <MudPaper @key="ingestPath" Class="pa-2" Elevation="0" Outlined="true">
                                <MudStack Row="true" AlignItems="AlignItems.Start" Spacing="2">
                                    <div class="flex-grow-1">
                                        <FolderPicker
                                            RootDirectory="@Environment.GetFolderPath(Environment.SpecialFolder.UserProfile)"
                                            SelectedPath="@ingestPath.Path"
                                            SelectedPathChanged="@(p => OnIngestPathChanged(index, p))"
                                            Title="@Loc["Title_IngestFolder"]" Required="true" />
                                    </div>
                                    <MudIconButton Icon="@Icons.Material.Filled.Delete" Color="Color.Error"
                                        Disabled="@(Library.IngestPaths.Count <= 1)"
                                        OnClick="@(() => RemoveIngestPath(index))"
                                        title="@Loc["Button_RemoveIngestPath"]" />
                                </MudStack>
                            </MudPaper>
                        }
                    </MudStack>
                    <MudButton Variant="Variant.Outlined" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Add"
                        OnClick="AddIngestPath" Class="mt-2">
                        @Loc["Button_AddIngestPath"]
                    </MudButton>
                </MudItem>
            </MudGrid>
        </MudTabPanel>

        <!-- Storage Paths Tab -->
        <MudTabPanel Text="@Loc["Tab_StoragePaths"]" Icon="@Icons.Material.Filled.FolderOpen">
            <MudGrid Spacing="4">
                @if (Library.MangaSeries.Count > 0)
                {
                    <MudItem xs="12">
                        <MudAlert Severity="Severity.Warning" Variant="Variant.Filled">
                            <div>
                                <MudText Typo="Typo.h6" Class="mb-2">
                                    @string.Format(Loc["Warning_LibraryContainsSeries"],
                                    Loc["Word_Series"].Value.ToLocalizedQuantity(Library.MangaSeries.Count,
                                    CultureInfo.CurrentUICulture))
                            </MudText>
                            <MudText Typo="Typo.body2">
                                @Loc["Warning_CannotChangePaths"]
                            </MudText>
                            <MudText Typo="Typo.body2" Class="mt-2">
                                @((MarkupString)Loc["Warning_CanStillChange"].Value)
                            </MudText>
                        </div>
                    </MudAlert>
                </MudItem>
                                }

                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_StorageLocations"]
                    </MudText>
                </MudItem>

                <MudItem md="6" xs="12">
                    <FolderPicker RootDirectory="@Environment.GetFolderPath(Environment.SpecialFolder.UserProfile)"
                        SelectedPath="@Library.NotUpscaledLibraryPath" SelectedPathChanged="@OnNotUpscaledPathChanged"
                        Disabled="Library.MangaSeries.Count > 0" Title="@Loc["Title_StoragePathAsIs"]"
                        Required="true" />
                </MudItem>

                <MudItem md="6" xs="12">
                    <FolderPicker RootDirectory="@Environment.GetFolderPath(Environment.SpecialFolder.UserProfile)"
                        SelectedPath="@Library.UpscaledLibraryPath"
                        Disabled="Library.MangaSeries.Count > 0 && Library.UpscaledLibraryPath != null"
                        SelectedPathChanged="@OnUpscaledPathChanged" Title="@Loc["Title_UpscaledLocation"]" />
                </MudItem>
            </MudGrid>
        </MudTabPanel>

        <!-- Upscaling Configuration Tab -->
        <MudTabPanel Text="@Loc["Tab_Upscaling"]" Icon="@Icons.Material.Filled.ImageAspectRatio">
            <MudGrid Spacing="4">
                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_UpscalingConfig"]</MudText>
                </MudItem>

                <MudItem xs="12">
                    <MudCheckBox T="bool" Label="@Loc["Label_UpscaleOnIngest"]" Color="Color.Primary"
                        @bind-Value="Library.UpscaleOnIngest" @bind-Value:after="OnInputChanged" />
                </MudItem>

                <MudItem xs="12">
                    <MudSelect T="int?" Label="@Loc["Label_UpscalerProfile"]"
                        HelperText="@Loc["Helper_UpscalerProfile"]" Immediate="true"
                        @bind-Value="Library.UpscalerProfileId" @bind-Value:after="OnInputChanged" Clearable="true"
                        Variant="Variant.Outlined">
                        <MudSelectItem T="int?" Value="null">@Loc["Option_None"]</MudSelectItem>
                        @foreach (var upscalerConfig in DbContext.UpscalerProfiles)
                        {
                            <MudSelectItem T="int?" Value="@upscalerConfig.Id">@upscalerConfig.Name</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>
            </MudGrid>
        </MudTabPanel>

        <!-- File Management Tab -->
        <MudTabPanel Text="@Loc["Tab_FileManagement"]" Icon="@Icons.Material.Filled.ManageSearch">
            <MudGrid Spacing="4">
                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_FileFiltering"]</MudText>
                </MudItem>

                <MudItem xs="12">
                    <EditLibraryFilters Library="@Library" />
                </MudItem>

                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_ChapterProcessing"]
                    </MudText>
                    <MudCheckBox T="bool" Label="@Loc["Label_MergeChapters"]" Color="Color.Warning"
                        @bind-Value="Library.MergeChapterParts" @bind-Value:after="OnInputChanged" />
                    <MudText Typo="Typo.caption" Color="Color.Warning">
                        @Loc["Warning_Experimental"]
                    </MudText>

                    <MudText Typo="Typo.h5" GutterBottom="true" Class="mt-4">
                        @Loc["Header_VerticalStrip"]</MudText>
                    <MudSelect T="StripDetectionMode" Label="@Loc["Label_DetectionMode"]"
                        HelperText="@Loc["Helper_DetectionMode"]" @bind-Value="Library.StripDetectionMode"
                        @bind-Value:after="OnInputChanged" Variant="Variant.Outlined">
                        <MudSelectItem Value="StripDetectionMode.None">@Loc["Option_Disabled"]
                        </MudSelectItem>
                        <MudSelectItem Value="StripDetectionMode.DetectOnly">@Loc["Option_DetectOnly"]
                        </MudSelectItem>
                        <MudSelectItem Value="StripDetectionMode.DetectAndApply">
                            @Loc["Option_DetectAndApply"]</MudSelectItem>
                    </MudSelect>
                </MudItem>
            </MudGrid>
        </MudTabPanel>

        <!-- Rename Rules Tab -->
        <MudTabPanel Text="@Loc["Tab_RenameRules"]" Icon="@Icons.Material.Filled.DriveFileRenameOutline">
            <MudGrid Spacing="4">
                <MudItem xs="12">
                    <MudText Typo="Typo.h5" GutterBottom="true">@Loc["Header_RenameConfig"]</MudText>
                    <MudText Typo="Typo.body1" Class="mb-4">
                        @Loc["Body_RenameConfig"]
                    </MudText>
                </MudItem>

                <MudItem xs="12">
                    <EditLibraryRenames Library="@Library" />
                </MudItem>

                <MudItem xs="12">
                    <MudDivider />
                    <div class="d-flex justify-center mt-4">
                        <MudButton Variant="Variant.Filled" Color="Color.Primary"
                            StartIcon="@Icons.Material.Filled.Preview" OnClick="ShowRenameDialog" Size="Size.Large">
                            @Loc["Button_PreviewRename"]
                        </MudButton>
                    </div>
                </MudItem>
            </MudGrid>
        </MudTabPanel>

        <!-- Integration Tab (only if Kavita is enabled) -->
        @if (KavitaConfig.Value.Enabled)
        {
            <MudTabPanel Text="@Loc["Tab_Integration"]" Icon="@Icons.Material.Filled.Hub">
                <MudGrid Spacing="4">
                    <MudItem xs="12">
                        <MudText Typo="Typo.h5" GutterBottom="true">
                            <MudIcon Icon="@Icons.Material.Filled.Hub" Class="mr-2" />
                            @Loc["Header_KavitaConfig"]
                        </MudText>

                        <MudAlert Severity="Severity.Info" Variant="Variant.Text" Class="mb-4">
                            <div>
                                <MudText Typo="Typo.body2">
                                    @Loc["Body_KavitaConfig"]
                                </MudText>
                                <MudText Typo="Typo.body2" Class="mt-2">
                                    @((MarkupString)Loc["Note_KavitaConfig"].Value)
                                </MudText>
                            </div>
                        </MudAlert>
                    </MudItem>

                    <MudItem md="6" xs="12">
                        <MudTextField Label="@Loc["Label_KavitaAsIs"]" HelperText="@Loc["Helper_KavitaAsIs"]"
                            Immediate="true" @bind-Value="Library.KavitaConfig.NotUpscaledMountPoint"
                            @bind-Value:after="OnInputChanged" Variant="Variant.Outlined" />
                    </MudItem>

                    <MudItem md="6" xs="12">
                        <MudTextField Label="@Loc["Label_KavitaUpscaled"]" HelperText="@Loc["Helper_KavitaUpscaled"]"
                            Immediate="true" @bind-Value="Library.KavitaConfig.UpscaledMountPoint"
                            @bind-Value:after="OnInputChanged" Variant="Variant.Outlined" />
                    </MudItem>

                    <MudItem xs="12">
                        <MudAlert Severity="Severity.Success" Variant="Variant.Text">
                            <MudText Typo="Typo.body2">
                                @((MarkupString)Loc["Hint_Kavita"].Value)
                            </MudText>
                        </MudAlert>
                    </MudItem>
                </MudGrid>
            </MudTabPanel>
        }
    </MudTabs>
</MudForm>

@code {

    [Parameter]
    public Library Library { get; set; } = new Library
    {
        Name = "",
    };

    [Parameter] public EventCallback<Library> LibraryChanged { get; set; }

    [Parameter] public bool IsValid { get; set; } = false;
    [Parameter] public EventCallback<bool> IsValidChanged { get; set; }

    private void IsValidChangedHandler(bool isValid)
    {
        IsValid = isValid;
        IsValidChanged.InvokeAsync(isValid);
    }

    protected override void OnInitialized()
    {
        // A library always needs at least one ingest path, so start editing with an empty one.
        if (Library.IngestPaths.Count == 0)
        {
            Library.IngestPaths.Add(new LibraryIngestPath());
        }
    }

    private void OnIngestPathChanged(int index, string newPath)
    {
        if (index < 0 || index >= Library.IngestPaths.Count)
        {
            return;
        }

        Library.IngestPaths[index].Path = newPath;
        OnInputChanged();
    }

    private void AddIngestPath()
    {
        Library.IngestPaths.Add(new LibraryIngestPath());
        OnInputChanged();
    }

    private void RemoveIngestPath(int index)
    {
        if (index < 0 || index >= Library.IngestPaths.Count || Library.IngestPaths.Count <= 1)
        {
            return;
        }

        Library.IngestPaths.RemoveAt(index);
        OnInputChanged();
    }

    private void OnNotUpscaledPathChanged(string newPath)
    {
        Library.NotUpscaledLibraryPath = newPath;
        OnInputChanged();
    }

    private void OnUpscaledPathChanged(string? newPath)
    {
        Library.UpscaledLibraryPath = newPath;
        OnInputChanged();
    }

    protected void OnInputChanged()
    {
        LibraryChanged.InvokeAsync(Library);
        IsValid = Validate();
        IsValidChanged.InvokeAsync(IsValid);
    }

    private bool Validate()
    {
        List<string> ingestPaths = Library.IngestPaths.Select(p => p.Path).ToList();

        bool requiredElementsValid = !string.IsNullOrWhiteSpace(Library.Name) &&
        ingestPaths.Count > 0 &&
        ingestPaths.All(p => !string.IsNullOrWhiteSpace(p)) &&
        !string.IsNullOrWhiteSpace(Library.NotUpscaledLibraryPath);

        bool ingestPathsAreUnique = ingestPaths.Distinct().Count() == ingestPaths.Count;

        bool pathsAreDifferent = ingestPathsAreUnique &&
        ingestPaths.All(p => p != Library.NotUpscaledLibraryPath && p != Library.UpscaledLibraryPath) &&
        Library.NotUpscaledLibraryPath != Library.UpscaledLibraryPath;

        bool pathsExist = ingestPaths.All(Directory.Exists) &&
        Directory.Exists(Library.NotUpscaledLibraryPath) &&
        (Library.UpscaledLibraryPath == null || Directory.Exists(Library.UpscaledLibraryPath));

        bool upscalingIsFullyConfiguredOrOff = !Library.UpscaleOnIngest ||
        (Library.UpscaleOnIngest && Library.UpscalerProfileId is not null &&
        !string.IsNullOrEmpty(Library.UpscaledLibraryPath));

        return requiredElementsValid && pathsAreDifferent && pathsExist && upscalingIsFullyConfiguredOrOff;
    }

    private async Task ShowRenameDialog()
    {
        var parameters = new DialogParameters { ["Library"] = Library, };
        var options = new DialogOptions
        {
            CloseButton = true,
            FullScreen = true
        };

        await DialogService.ShowAsync<LibraryRenameDialog>("Edit & Preview Rename Rules", parameters, options);
    }

}